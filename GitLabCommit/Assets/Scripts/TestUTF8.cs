using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestUTF8 : MonoBehaviour
{
    // UTF-8 타입 적용 테스트
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("UTF-8 타입으로 변경 적용 테스트");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
